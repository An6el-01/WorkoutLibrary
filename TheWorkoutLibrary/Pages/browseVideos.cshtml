@page
@using TheWorkoutLibrary.Data
@model TheWorkoutLibrary.Pages.browseVideosModel
@{
    ViewData["Title"] = "Browse Videos";
    Layout = "~/Pages/_Layout.cshtml";
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<link rel="stylesheet" href="https://fonts.sandbox.google.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />

<form method="POST">
    <div class = "browseVideo">
    <h1>BrowseVideos</h1>

    <table>
        <thead>
        <th>Video Table</th>
        </thead>

        
    <tbody>
    @foreach (var WO in Model.Workout)
    {

        <tr>

            @{
                try
                {
                    <td>
                        <video width="160" height="283" controls muted>
                            <source src="@string.Format("data:video/mp4;base64,{0}", Convert.ToBase64String(WO.videoData))" type="video/mp4"/>

                        </video>
                    </td>
                }
                catch
                {
                    <td>
                        <source src="/Images/empty.png" alt="No Image Provided"/>
                    </td>
                }
            }


            <td> <h2>@WO.workoutName</h2>
                
                <h3>@WO.workoutDifficulty</h3> 
                
                @if (@WO.workoutDifficulty == "Beginner")
                {
                    <div class = "material-symbols-outlined1">
                    <span class="material-symbols-outlined">
                        fitness_center
                    </span>
                    </div>
                } else if (@WO.workoutDifficulty == "Intermediate")
                {
                    <div class = "material-symbols-outlined2">
                    <span class="material-symbols-outlined">
                        fitness_center
                    </span>
                    </div>
                } else if (@WO.workoutDifficulty == "Advanced")
                {
                    <div class = "material-symbols-outlined3">
                    <span class="material-symbols-outlined">
                        fitness_center
                    </span>
                    </div>
                }
                
            
                </td>

        </tr>
    }
    </tbody>
    </table>
        </div>





    
</form>

